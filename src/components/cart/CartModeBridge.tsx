"use client";

import { useEffect } from "react";

export default function CartModeBridge() {
  useEffect(() => {
    // Starter bridge: keep placeholder for future cart sync logic.
  }, []);

  return null;
}
